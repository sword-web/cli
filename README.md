# Sword CLI

`sword-cli` provides a command line interface for scaffolding Sword projects.

- `sword`
- `swd` (short alias)

Both commands expose the same CLI surface.

## Install

```bash
cargo install --path .
```

## `sword new`

Scaffolds a basic Sword project in a new directory.

```bash
sword new my-app
```
